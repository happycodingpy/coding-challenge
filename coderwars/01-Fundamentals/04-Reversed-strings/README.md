# Reversed Strings

Description:

Complete the solution so that it reverses the string passed into it.

```javascript
'world'  =>  'dlrow'
```

Solutions:

```javascript
const solution = str => str.toString().split('').reverse().join('');
```
