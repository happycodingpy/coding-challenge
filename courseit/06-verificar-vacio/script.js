// Verificar vacío
// Queremos que la función isEmpty devuelva true si el string está vacío, y false si no lo está
const str = '';
const isEmpty = str => (str === '' ? true : false);

const a = isEmpty(str);
console.log(a);
